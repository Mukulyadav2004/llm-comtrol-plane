# Test-only dependencies. Service runtime deps live in services/*/requirements.txt.
pytest==8.3.3
pytest-asyncio==0.24.0
fakeredis==2.26.1
lupa==2.2                 # Lua scripting support for fakeredis (rate limiter)
httpx==0.27.0             # fastapi.testclient transport
